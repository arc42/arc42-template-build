# Custom Fonts

This directory is reserved for custom fonts that may be needed for specific language builds.

For the PoC (EN, DE only), the default system fonts are sufficient.

## Future Use

When adding support for languages requiring special fonts (e.g., Chinese, Russian, Ukrainian), place the font files here and update the Dockerfile to install them.
